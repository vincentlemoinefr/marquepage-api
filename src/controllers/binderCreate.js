export default function binderCreate(request, response, next) {
  // Not yet implemented

  // What need to be done here
  // We know the user is authentified (we have his sub)
  // We got his data, we need to validate it with joi
  // Call the database
  // Return 200 with data
  // On error 422




  response.status(500).end();
};